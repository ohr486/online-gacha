<%= form_for([:admin, @tweet_template]) do |form| %>
  <% if tweet_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet_template.errors.count, "error") %> prohibited this tweet_template from being saved:</h2>

      <ul>
      <% tweet_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tweet_day %>
    <%= form.date_select :tweet_day %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_area :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
